name: flutter_driver_mcp
description: A custom MCP server for Flutter Driver - works around dart-lang/sdk#62265
version: 1.0.0
repository: https://github.com/nannantown/flutter-driver-mcp

environment:
  sdk: ^3.10.4

dependencies:
  args: ^2.6.0
  web_socket_channel: ^3.0.0
  vm_service: ^15.0.0
  async: ^2.11.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  flutter_driver_mcp: server
